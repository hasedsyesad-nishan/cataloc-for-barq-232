# دليل حملة برق الإعلانية — ملف التسليم الكامل (Handoff)

> انقل هذا الملف كاملاً لأي محادثة جديدة. يحتوي كل ما يلزم لمتابعة العمل بلا فقدان أي معلومة: طريقة الإنتاج التقنية، الأصول، القواعد، ما أُنجز، وما تبقّى مع سيناريوهاته.

---

## 1) نبذة عن المشروع

- **المنتج:** نظام «برق / BARQ» — نظام سوري ذكي لمراقبة وإدارة الطاقة الشمسية المنزلية.
- **الأجهزة الأربعة:**
  1. **المرسلة:** قلب النظام، تتصل بالإنفرتر (RS-232/RJ45، مخرج COMM)، تقرأ بيانات البطارية/الشمس لحظياً، تبثّها لاسلكياً (ESP-NOW) وترفعها عبر WiFi. تغذية DC 12V.
  2. **جهاز البراد:** يشغّل/يوقف البراد تلقائياً حسب نسبة شحن البطارية. ريلي 10 أمبير. **يمكن تركيبه بأي مصدر كهربائي** (كان مكتوباً «بيلزق» وتم تصحيحه — ممنوع استخدام «بيلزق»).
  3. **جهاز السخان:** يسخّن الماء من **فائض** إنتاج الألواح الشمسية فقط، **ولا يأخذ من البطارية ولا (1) أمبير**. تحكم تدريجي. شاشة رقمية (فولت/أمبير). يعمل بأمان مع أي سخان حراري (سلك حراري/أومي).
  4. **جهاز المكيف:** للأحمال الكبيرة (مكيفات/غسالات)، **يتحمل حتى 30 أمبير**، تركيب ثابت بترمينالات.
- **رقم الشركة (يُختم به كل فيديو):** `0936177050`
- **الهوية:** «صناعة سورية» + العلم السوري الجديد (انظر القواعد).
- **المستودع/الفرع:** `hasedsyesad-nishan/cataloc-for-barq-232` — الفرع `claude/barq-video-ad-tq9uls`. كل الأصول والفيديوهات مرفوعة هناك.
- **المولّد الحالي:** claude-opus-4-8. لا تذكر هوية الموديل في أي ملف/كوميت مرفوع.

### قيود البيئة (مهمة جداً)
- البيئة سحابية معزولة: **كل المواقع الخارجية محجوبة** (imagine.art, pika, kling, google, claude.ai/design، أي رابط). لا يمكن فتح روابط أو استخدام مولّدات خارجية.
- **غوغل درايف محجوب** أيضاً. لاستلام صور المستخدم: يرسلها في المحادثة كـ ZIP (تصل كملف حقيقي)، أو يرفعها على GitHub.
- الصور المعروضة inline في المحادثة **تُرى بصرياً فقط ولا تُحفظ كملفات** — اطلب ZIP دائماً.
- الحاوية تُعاد تشغيلها أحياناً؛ ملفات scratchpad قد تبقى أو تُمسح — تحقق دائماً وأعد التثبيت إن لزم.

---

## 2) طريقة صنع الفيديو (المحرك التقني الكامل)

كل الفيديوهات موشن-غرافيك عمودية **1080×1920 @ 24fps**، تُبنى داخل البيئة بلا أي أداة خارجية.

### أ) تثبيت الأدوات (إن كانت الحاوية جديدة)
```bash
pip install -q imageio-ffmpeg pillow numpy playwright
# خطوط Cairo العربية (نسخة 0.2.3 تحديداً فيها ملفات ttf):
cd <scratchpad> && npm pack @expo-google-fonts/cairo@0.2.3 && tar xzf expo-google-fonts-cairo-0.2.3.tgz
# مجلد package/ سيحوي Cairo_700Bold.ttf, Cairo_600SemiBold.ttf, Cairo_400Regular.ttf
# خط Amiri للنصوص الأدبية:
npm pack @expo-google-fonts/amiri@0.2.3 && mkdir -p amiri && tar xzf expo-google-fonts-amiri-0.2.3.tgz -C amiri
# متصفح Chromium مثبّت مسبقاً في: /opt/pw-browsers/chromium  (لا تشغّل playwright install)
# تأكد أن PIL يدعم raqm للعربي: python3 -c "from PIL import features; print(features.check('raqm'))"  # يجب True
```
ملاحظات:
- ffmpeg يُستدعى عبر: `python3 -c "import imageio_ffmpeg; print(imageio_ffmpeg.get_ffmpeg_exe())"`
- خط `NotoColorEmoji` متاح للإيموجي.
- صورة حبيبات الفيلم `editorial/grain.png` تُصنع مرة: مصفوفة numpy عشوائية رمادية (Gaussian noise) بحجم ~1200×675، تُحفظ PNG.

### ب) بناء المشهد (HTML/CSS)
- كل مشهد صفحة HTML بمقاس 1080×1920 (أو 1350×2400 ثم يُقصّ) مع `dir="rtl" lang="ar"`.
- النص العربي: استخدم `direction:rtl` — Playwright/Chromium يشكّل العربي صحيحاً (raqm غير مطلوب لأن المتصفح يشكّل).
- الأنيميشن: **CSS animations** (keyframes): انزلاق النص للأعلى مع تأخيرات متدرجة `animation-delay`, `cubic-bezier(.16,1,.3,1)`, مسح ضوئي (sweep), طفو (floaty), عدّاد أرقام (odometer via translateY), امتلاء بطارية (width), توهج (glow), إلخ.

### ج) تصوير الإطارات (الطريقة المعتمدة — دقيقة وحتمية)
Playwright يفتح الصفحة، ويثبّت زمن الأنيميشن لكل إطار ثم يلتقط سكرين شوت:
```python
from playwright.sync_api import sync_playwright
FPS=24
with sync_playwright() as p:
    b=p.chromium.launch(executable_path='/opt/pw-browsers/chromium')
    pg=b.new_page(viewport={'width':1080,'height':1920})
    pg.goto(f"file://{scene}.html"); pg.wait_for_timeout(400)
    for i in range(int(dur*FPS)):
        pg.evaluate("t => { document.getAnimations().forEach(a => { a.pause(); a.currentTime = t; }); }", i*1000/FPS)
        pg.screenshot(path=f"frames/{i:04d}.png")
```
- للأوفرلي الشفاف (نص فوق صورة): `pg.screenshot(..., omit_background=True)` مع `background:transparent`.

### د) الترميز والدمج (ffmpeg)
```python
# كل مشهد -> clip بـ fade in/out:
ff -y -framerate 24 -i frames/f%04d.png -vf "fade=t=in:st=0:d=0.3,fade=t=out:st=<dur-0.3>:d=0.3,format=yuv420p" \
   -c:v libx264 -preset medium -crf 19 -r 24 clip.mp4
# دمج المشاهد: ملف list.txt فيه أسطر  file 'clips/A.mp4'  ثم:
ff -y -f concat -safe 0 -i list.txt -c copy final.mp4
```

### هـ) تحذيرات أداء مهمة (تعلّمناها بالتجربة)
- **zoompan في ffmpeg بطيء جداً** على صور كبيرة (دقائق للمشهد). للصور الفوتوغرافية بملء الشاشة: استخدم **صورة ثابتة + overlay نص شفاف + fades** (سريع ~ثوانٍ)، أو حرّك عبر CSS ثم صوّر إطارات.
- **فلاتر التوهج SVG (feGaussianBlur)** تُبطئ تصوير Playwright (~100-115 ثانية/مشهد بدل ~13 ثانية). الروبوت المستقبلي بطيء لهذا السبب — اصبر أو شغّل بالخلفية (`run_in_background`).
- المشاهد النصية البسيطة/الروبوت الكرتوني: سريعة (~10-13 ثانية/مشهد).
- **التحقق البصري إلزامي:** استخرج عدة إطارات (`ff -ss <t> -i final.mp4 -frames:v 1 chk.jpg`) واقرأها بأداة Read قبل التسليم. انتبه لقصّ النص عند الحواف، وتداخل النص مع الفوتر.

### و) التسليم
- احفظ الفيديو مرقّماً في `فيديوهات_جاهزة/` بالمستودع، اعمل commit وpush على الفرع، ثم أرسله للمستخدم عبر `SendUserFile` (status=proactive للمهام المجدولة).
- **حد رفع الملفات 30 ميغا** — الملفات الأكبر قسّمها (زيبّها على دفعتين). أسماء الملفات العربية قد تحتاج تمريرها كـ unicode escapes في SendUserFile.

---

## 3) الأصول الموجودة في المستودع

### الصور الأصلية (كتالوج) — `صور_الدعاية/`
- `المرسلة_3_زوايا.png` · `جهاز_البراد_3_زوايا.jpeg` · `جهاز_المكيف_3_زوايا.png` · `جهاز_السخان_مجموعة.png`
- سكرينات التطبيق: `سكرين_لوحة_التحكم.jpg` · `سكرين_انشاء_حساب.jpg` · `سكرين_ضبط_السخان.jpg`

### الصور الفوتوغرافية الفاخرة (من المستخدم عبر ZIP) — `صور_الدعاية/صور_فوتوغرافية/`
- `اللوغو_المعدني.png` (لوغو BARQ ثلاثي الأبعاد معدني)
- `المرسلة_مكتب.png` · `السخان_مكتب.png` · `البراد_مكتب.png` · `المكيف_مكتب.png` (كل جهاز في مكتب فاخر بإطلالة مدينة)

### صور بطولية معالجة — `صور_الدعاية/صور_احترافية/`
- نسخ معزولة/متوهجة صنعتها من الكتالوج + `السخان_مفرد_من_ديزاين.png` (مستخرج من لوحة Design).

### أصول scratchpad مفيدة (قد تحتاج إعادة إنشائها)
- `heater_float.png` (سخان معزول من Design), `wow/fridge_cut.png` (براد معزول نظيف), `wow/trans_embed.png`, `wow/*_بطولية`.
- `future/robot_def.py` (تعريف الروبوت المستقبلي SVG — انظر القسم 6).

### ملفات توثيق موجودة
- `خطة_فيديوهات_الدعاية_BARQ.md` (البرومبتات + قواعد الإنتاج + قواعد المصداقية)
- `برومبتات_كلود_ديزاين.md` / `برومبتات_كلود_ديزاين_2_سينمائي.md` / `برومبتات_صور_فردية_للاجهزة.md`
- `قائمة_التصوير_بالهاتف.md` · `خطة_30_يوم_محتوى_برق.md` · `خطة_برق_15فيديو_40صورة.md`
- صور منشورات: `صور_منشورات/` (اقتباسات + إنفوغرافيك 1080×1080)

---

## 4) القواعد الإلزامية (مصداقية + لياقة + هوية)

1. **لا تذكر المدى بالأمتار** (مثل ٢٠٠م) — المدى غير ثابت (يعتمد الجدران). استخدم «لاسلكي» أو «ESP-NOW».
2. **السخان:** لا تذكر «ترياك/BTA40» — قل «يتحمل حتى 30 أمبير» (هذه لجهاز المكيف فعلياً؛ السخان استخدم صياغات الفائض والحماية). الميزة الأقوى للسخان: **لا يأخذ من البطارية ولا (1) أمبير**.
3. **الرقم 1 داخل نص عربي:** اكتبه لاتينياً وبين قوسين `(1)` حتى لا يُقرأ كألف. (أو استخدم كلمة «واحد»).
4. **ممنوع «بيلزق»** (غير لائقة) — البديل: «يمكن تركيبه بأي مصدر كهربائي».
5. **ممنوع «عقل صغير»** (تُفهم كإهانة) — البديل: «عقلٌ كبير… بحجمٍ صغير».
6. **العلم السوري الجديد** (ممنوع إيموجي 🇸🇾 القديم): يُرسم SVG — 3 أشرطة أفقية أخضر `#007A3D` / أبيض / أسود، و**ثلاث نجوم حمراء خماسية** `#CE1126` في الشريط الأبيض. كود SVG جاهز:
```html
<svg width="66" height="44" viewBox="0 0 90 60"><rect width="90" height="20" fill="#007A3D"/><rect width="90" height="20" y="20" fill="#fff"/><rect width="90" height="20" y="40" fill="#000"/>
<polygon points="24,23.5 25.53,28.2 30.47,28.2 26.47,31.1 28,35.8 24,32.9 20,35.8 21.53,31.1 17.53,28.2 22.47,28.2" fill="#CE1126"/>
<polygon points="45,23.5 46.53,28.2 51.47,28.2 47.47,31.1 49,35.8 45,32.9 41,35.8 42.53,31.1 38.53,28.2 43.47,28.2" fill="#CE1126"/>
<polygon points="66,23.5 67.53,28.2 72.47,28.2 68.47,31.1 70,35.8 66,32.9 62,35.8 63.53,31.1 59.53,28.2 64.47,28.2" fill="#CE1126"/></svg>
```
7. **لا سعر في أي فيديو** — الهدف الاتصال بالرقم `0936177050`.
8. **لا تعليق صوتي** من المولّدات (تنطق العربي خطأً) — النص مكتوب فقط، والتعليق (إن لزم) يُسجّل خارجياً (ElevenLabs).
9. اختم **كل** فيديو بشاشة فيها الرقم + «صناعة سورية» + العلم الجديد.

---

## 5) الهويّات البصرية الثلاث المعتمدة

1. **الحركي (Kinetic/Editorial):** خلفية داكنة فحمية، ذهبي `#d9a441`، خط Cairo، حبيبات فيلم + vignette، نصوص تنزلق، شرائح مواصفات، عدّادات، شاشة ختامية ذهبية بزر نابض. (فيديوهات 1-7, 17-20).
2. **العاطفي الأدبي (Cinematic):** أسود، خط **Amiri** الكلاسيكي، لغة فصحى شعرية (تشبيه/استعارة)، إيقاع بطيء وتلاشٍ طويل (fade 0.6-0.7s)، توقيت قراءة مريح (مشاهد 5-7 ثوانٍ). (فيديوهات 8-16). **درس مهم:** أعطِ وقت قراءة كافياً — يمكن تبطئة فيديو جاهز بـ `setpts=1.22*PTS`.
3. **الروبوت (شخصية شارحة):**
   - **بَرقوق الكرتوني (2020):** SVG لطيف، خلفية نهارية كرتونية (شمس/عشب/غيوم)، فقاعات كلام، بطاقة بيضاء فيها المنتج (بأسلوب فيكتور: PIL posterize مستوى 4 + saturation 1.5). سريع الإنتاج. (فيديو 21).
   - **روبوت المستقبل (2225):** SVG معدني هولوغرافي (كروم + زجاج + عين فايزر سماوية `#5fd0ff` + نواة طاقة + hover glow)، بيئة سايفاي داكنة بشبكة هولوغرام، المنتج داخل «ماسح ضوئي» بخطوط مسح، خط Amiri، لغة راقية جديدة. **بطيء الإنتاج** (فلاتر glow). (فيديو 22).

---

## 6) كود الروبوت المستقبلي (احفظه — أساس الفيديوهات القادمة)

الملف `future/robot_def.py` يحوي متغير `ROBOT` (SVG قابل للتنسيق بـ `.format(w=...)`). مواصفاته: دوم رأس كروم بتدرج `chrome`، وجه زجاجي `glass`، عين فايزر أفقية متوهجة `visor` بفلتر `glow` (feGaussianBlur stdDeviation=7)، نواة طاقة `core` ببرق أبيض، أذرع طافية، وسادة hover سفلية متوهجة. الأنيميشن (CSS): `.floatbot` طفو، `.armL/.armR` تأرجح، `.visor` نبض، `.hoverpad` توسّع.

**بيئة المشهد (bg):** تدرج شعاعي أزرق داكن `#0d2140→#03060d`، أرضية شبكة هولوغرام (repeating-linear-gradient سماوي مع mask تلاشٍ)، توهج محيط، شعار BARQ أعلى اليسار.

**إطار المنتج الهولوغرافي (holo_product):** حدود سماوية `rgba(90,220,255,.55)`، توهج داخلي/خارجي، خط مسح `.scan` متحرك، زوايا L-brackets في الأركان الأربعة.

> عند إعادة الاستخدام في محادثة جديدة: أعد إنشاء `future/robot_def.py` من الوصف أعلاه (أو من فيديو 22 المرفوع)، وأصلح تدرج `chrome2` (كان فيه خطأ `#5f7a9e`).

---

## 7) الفيديوهات المُنجزة (22 فيديو في `فيديوهات_جاهزة/`)

| # | الاسم | الأسلوب | الموضوع |
|---|-------|---------|---------|
| 1 | تعريفي_بالنظام | حركي | تعريف عام (أُعيد بناؤه بالبطولية) |
| 2 | اقناعي_انقطاع_الكهرباء | حركي | معاناة الانقطاع + الحل |
| 3 | البراد_التشغيل_التلقائي | حركي | البراد 70%/40% (صُحّح «بيلزق») |
| 4 | يوم_مع_برق_سينمائي | حركي | قصة من الفجر للّيل |
| 5 | السخان_فائض_الشمس | حركي | السخان + لا (1) أمبير |
| 6 | السخان_احترافي | حركي | السخان معزول من Design |
| 7 | المراقبة_عن_بعد | حركي | لوحة التحكم/التطبيق |
| 8 | البراد_الحارس_الصامت | عاطفي | «حارسك الصامت» (مبطّأ) |
| 9 | السخان_ما_وهبته_الشمس | عاطفي | كتابة أدبية (مبطّأ) |
| 10 | المرسلة_العين_التي_لا_تنام | عاطفي | «العين التي لا تنام» |
| 11 | عرض_فاخر_بالصور_الفوتوغرافية | فوتوغرافي | لوغو + 4 أجهزة (صُحّح «عقل صغير») |
| 12 | المرسلة_منفرد | فوتوغرافي | 3 بيتات + ختام |
| 13 | السخان_منفرد | فوتوغرافي | 3 بيتات |
| 14 | البراد_منفرد | فوتوغرافي | 3 بيتات |
| 15 | المكيف_منفرد | فوتوغرافي | 3 بيتات |
| 16 | بيت_يتنفس_عاطفي | عاطفي فوتوغرافي | الفيلم الرئيسي (الحي عتمة إلا بيتك) |
| 17 | المكيف_حركي | حركي | صورة أصلية، 30 أمبير |
| 18 | البراد_حركي | حركي | صورة أصلية (صُحّح «بيلزق») |
| 19 | المرسلة_حركي | حركي | صورة أصلية |
| 20 | حماية_البطارية_والتوفير | حركي | حماية+توفير (مهمة يومية) |
| 21 | روبوت_برقوق_السخان | روبوت كرتوني | بَرقوق يشرح السخان |
| 22 | روبوت_المستقبل | روبوت مستقبلي | لغة راقية جديدة + هولوغرام |

كما يوجد `نموذج_الاسلوب_الجديد_موشن.mp4` (عيّنة تجريبية).

---

## 8) الطلب الحالي غير المنفّذ (ابدأ من هنا) ⬅️

**المستخدم يريد: عدة فيديوهات أخرى بنفس روبوت المستقبل (فيديو 22) لشرح كل المنتجات — فيديو منفصل لكل جهاز.**

المطلوب إنتاج (بنفس أسلوب فيديو 22 المستقبلي، لغة راقية جديدة غير معتادة):
- **روبوت المستقبل × المرسلة** — سيناريو مقترح: «العقل الذي يرى» — المرسلة تقرأ كل نبضة طاقة. لغة: «في صمتها… تُنصت لكل ذرّة» / «تعرف قبل أن تعرف».
- **روبوت المستقبل × البراد** — «الحارس الذي لا يغفل» — يحمي الغذاء والبطارية. «يقظٌ حين ينام الجميع».
- **روبوت المستقبل × السخان** — «كيمياء الضوء» — يحوّل فائض الشمس لدفء بلا أن يمسّ البطارية. «يأخذ ما كان سيضيع… ويهبك دفئاً».
- **روبوت المستقبل × المكيف** — «القوة المروّضة» — 30 أمبير تحت سيطرة ذكية.

لكل فيديو: مشهد ظهور الروبوت + 2-3 مشاهد (المنتج داخل الماسح الهولوغرافي بصوره الفوتوغرافية + لغة راقية) + شاشة ختام بالرقم والعلم. استخدم صور `صور_الدعاية/صور_فوتوغرافية/` داخل `holo_product`. رقّمها 23، 24، 25، 26.

**تنبيه إنتاجي:** روبوت المستقبل بطيء (~110 ثانية/مشهد بسبب فلاتر glow). شغّل التصوير بالخلفية (`run_in_background: true`) وراقبه، أو قلّل عدد المشاهد. تحقق بصرياً قبل التسليم.

---

## 9) سيناريوهات/أفكار أخرى لم تُنفّذ بعد (مخزون)

- **فيديو المنظومة الكاملة** (الأجهزة الأربعة تعمل معاً كفريق).
- **فيديو التطبيق خطوة بخطوة** (سكرينات إنشاء الحساب + ضبط السخان + لوحة التحكم).
- **فيديو «قبل / بعد»** (انقسام شاشة: بيت بلا برق / بيت مع برق).
- **فيديو شهادة عميل (UGC)** — يحتاج صورة وجه (Gemini/D-ID) — لم يُنفّذ (البيئة تمنع الأدوات الخارجية؛ ينتظر صورة من المستخدم).
- **40 صورة منشورات** (اقتباسات + إنفوغرافيك + منتجات + مشاهد) — أُنجز منها 7 فقط في `صور_منشورات/`.
- **سلسلة بَرقوق الكرتوني** لباقي المنتجات (المرسلة/البراد/المكيف) — أُنجز السخان فقط.
- **الفيديوهات العاطفية الأدبية** لمواضيع جديدة (رسالة أب، ليلة شتاء، الشمس لا تُرسل فاتورة).
- **صور فردية معزولة** للمرسلة/البراد/المكيف من Design (السخان فقط أُنجز) — لتحسين الفيديوهات.

## 10) المهمة اليومية المجدولة
هناك Routine مجدولة (cron) تنتج **فيديو حركي واحد يومياً** الساعة ~11 صباحاً بتوقيت سوريا، تنوّع الموضوع بالتسلسل، وتتحقق من آخر رقم في `فيديوهات_جاهزة/`. آخر ما أنتجته: فيديو 20. تلتزم بكل القواعد أعلاه.

## 11) تفضيلات المستخدم (ملخّص سلوكي)
- يحب التطوير المستمر والجرأة في الأفكار؛ يعطي ملاحظات دقيقة (لغوية/ثقافية) — نفّذها فوراً وثبّتها كقاعدة.
- يفضّل الكلام **البسيط أحياناً** (روبوت مرح) و**الأدبي الراقي أحياناً** (سينمائي) — اسأل/طابق حسب الطلب.
- حسّاس للياقة اللغوية باللهجة السورية (تجنّب أي لفظ قد يُساء فهمه).
- يريد دائماً «المزيد» و«الأجمل» و«غير المعتاد».
- يرسل صوره الجديدة كـ ZIP في المحادثة.
